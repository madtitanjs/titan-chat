import { PreviewDatePipe } from './preview-date.pipe';

describe('PreviewDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewDatePipe();
    expect(pipe).toBeTruthy();
  });
});
